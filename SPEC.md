We are building an app where a doctor inputs a FASTA file that contains DNA data about a client
then it goes through a model inference call of our classifier model that gives a score
between 0 and 1 and that determines how much the antibiotic drug they take will be effective for the client

the app needs to use next.js for the frontend
the backend will be convex
we will call huggingface endpoints, thats where I will deploy the model for inference

now implement all of this, and include the animation that we have for after the user has supplied the fasta file
and is waiting for infarance, and make sure that the whole animation has gone through.

The design should be clean and black and white,
we should use a sleek font like switzer the one I added to the Fonts folder
